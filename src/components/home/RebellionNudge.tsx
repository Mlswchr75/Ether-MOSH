/** Bottom-right manifesto whisper. Pure decor — pointer events stay off. */
export const RebellionNudge = () => (
  <div
    aria-hidden="true"
    className="pointer-events-none absolute bottom-[7%] right-[5%] z-10 hidden max-w-[180px] rotate-[2deg] text-right font-mono text-[9px] uppercase leading-relaxed tracking-[0.3em] text-foreground/35 md:block"
  >
    treat every pixel
    <br />
    as negotiable
  </div>
);
